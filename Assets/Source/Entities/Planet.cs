namespace Assets.Source.Entities
{
    public class Planet : MapActor
    {
        // Use this for initialization
        private new void Start()
        {
            base.Start();
        }

        // Update is called once per frame
        private void Update()
        {
        }
    }
}